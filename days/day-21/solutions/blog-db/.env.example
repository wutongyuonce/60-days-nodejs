# 复制为 .env 后再使用：cp .env.example .env
# .env 不要提交到 Git（已在仓库根 .gitignore 排除）

POSTGRES_USER=blog
POSTGRES_PASSWORD=blog_dev_pwd
POSTGRES_DB=blog
POSTGRES_PORT=5432

# 应用层连接串（Day 25 Prisma 会用）
DATABASE_URL=postgresql://blog:blog_dev_pwd@localhost:5432/blog?schema=public
